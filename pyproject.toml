[project]
name = "evostream-ga-sentiment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "confluent-kafka>=2.14.0",
    "loguru>=0.7.3",
    "pandas>=3.0.2",
    "pydantic-settings>=2.14.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
